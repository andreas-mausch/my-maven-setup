micronaut:
  application:
    name: kotlin-micronaut-example
  server:
    netty:
      access-logger:
        enabled: true
        exclusions:
          - ^/health(?:/.*)?$
  router:
    static-resources:
      docs:
        paths: classpath:META-INF/swagger/views/rapidoc
        mapping: /docs/**
      openapi:
        paths: classpath:META-INF/swagger
        mapping: /docs/spec/**

rabbitmq:
  queues:
    order-completed: orders.completed

webhooks:
  order-completed:
    url: ${ORDER_COMPLETED_WEBHOOK_URL}
